<div class="comment-section">
  {{#each this}}
    <section class="comment-container">
      <div class="meta">
        {{user.username}}
        Posted on:
        {{format_date created_at}}
      </div>
      <div class="text">
        {{comment_text}}
      </div>
    </section>
  {{/each}}
</div>